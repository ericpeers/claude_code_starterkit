# Copy to .stage.env and fill in real values. .env / *.env are gitignored.
# Example remote-host settings for your own deploy/admin scripts. These are
# placeholders — add, rename, or remove them to match the scripts you write.

STAGE_HOST=
STAGE_USER=
STAGE_SSH_KEY=
STAGE_DOMAIN=
