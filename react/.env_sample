# Copy to .env and fill in real values. .env is gitignored — never commit secrets.

# Use mock data/services instead of a live backend (true | false).
VITE_USE_MOCK=true

# Base URL for the backend API in production builds.
# VITE_API_BASE_URL=https://api.example.com
